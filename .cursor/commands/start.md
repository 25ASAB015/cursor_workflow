# /start

Propósito: Iniciar un nuevo work item y preparar rama(s) usando GitKraken CLI.

Uso básico:
```bash
./.cursor/bin/start.sh "{name}" [opciones]
```

Ayuda completa:
```bash
./.cursor/bin/start.sh --help
```

